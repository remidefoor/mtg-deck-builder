<section class="col-6">
    <h3>Deck</h3>

    @if (Deck != null)
    {
        @foreach (CardReadDTO card in Deck)
        {
            <img src="@card.ImageUrl" alt="@card.Name" @onclick="@(() => RemoveCardFromDeck(card))" />
        }
    }
</section>

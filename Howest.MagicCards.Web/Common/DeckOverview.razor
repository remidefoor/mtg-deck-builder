<section class="col-6">
    <h3>Deck</h3>

    @if (Deck != null)
    {
        <ul>
            @foreach (DeckCardReadDetailDTO deckCard in Deck)
            {
                <li @onclick="@(() => RemoveCardFromDeck(deckCard))">@deckCard.Name: @deckCard.Amount</li>
            }
        </ul>
    }
</section>

<section>
    <h3>Cards</h3>

    @if (Cards != null)
    {
        @foreach (CardReadDTO card in Cards)
        {
            <img src="@card.ImageUrl" alt="@card.Name" />
        }
    }
</section>
